[pytest]
pythonpath = src
testpaths = tests
addopts = -q -m "not rag"
markers =
    contract: contract-level tests (execution / structure / guarantee)
    rag: RAG semantic tests (excluded from CI by default)
