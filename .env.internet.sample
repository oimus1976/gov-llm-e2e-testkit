# ==========================================================
# gov-llm-e2e-testkit - INTERNET profile 専用テンプレ
# (.env.internet.sample)
# ==========================================================
# INTERNET プロファイル（ENV_PROFILE=internet）で実行する場合に
# 必要となる Secrets のテンプレートです。
#
# このファイルをコピーして `.env.internet` を作成してください。
#
#   cp .env.internet.sample .env.internet
#
# ----------------------------------------------------------
# 🔐 注意
# - 本番アカウントは書かない。必ずテスト用アカウントを使用してください。
# - GitHub やリポジトリには絶対に commit しない (.env.* は ignore 必須)
# ----------------------------------------------------------


# Qommons（INTERNET 側）接続 URL
QOMMONS_URL=https://qommons.ai

# Qommons（INTERNET 側）テスト用アカウント
# ※ここに書くのは必ず「テスト用」アカウント
QOMMONS_USERNAME=
QOMMONS_PASSWORD=
