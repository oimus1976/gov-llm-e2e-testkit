# ==========================================================
# gov-llm-e2e-testkit - dotenv 共通テンプレ (.env.sample)
# ==========================================================
# このファイルは Secrets を含まない雛形です。
# ユーザーはこのファイルをコピーして .env または .env.<profile> として利用します。
#
#   cp .env.sample .env.internet
#
# などのようにプロファイルに合わせてファイル名を変更してください。
#
# ----------------------------------------------------------
# 🔐 注意（セキュリティ）
# - 本番アカウント情報を絶対に書かない
# - GitHub に commit しない (.env / .env.* は .gitignore により除外)
# - CI では secrets.* が優先されるため .env は不要
# ----------------------------------------------------------


# ===============================
# INTERNET profile 用テンプレ例
# ===============================

# Qommons (INTERNET 側) の接続先 URL
QOMMONS_URL=

# INTERNET 側のテスト用アカウント
QOMMONS_USERNAME=
QOMMONS_PASSWORD=


# =====================================
# LGWAN profile を使う場合のテンプレ
# （必要に応じて .env.lgwan を作成）
# =====================================
QOMMONS_LGWAN_URL=
QOMMONS_LGWAN_USERNAME=
QOMMONS_LGWAN_PASSWORD=
